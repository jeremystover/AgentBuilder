/**
 * web/auth.js — thin re-export of @agentbuilder/web-ui-kit auth helpers.
 * Kept as a per-agent module so worker.js imports stay short and any
 * agent-specific session policy can be layered here later.
 */

export {
  createSession,
  destroySession,
  readSessionFromRequest,
  setSessionCookieHeader,
  clearSessionCookieHeader,
  requireWebSession,
  requireApiAuth,
  verifyPassword,
  WEB_AUTH_CONST,
} from "@agentbuilder/web-ui-kit";
