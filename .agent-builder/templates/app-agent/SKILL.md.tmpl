# __AGENT_NAME__

**Purpose.** __AGENT_PURPOSE__

This is an **app-agent**: it serves a UI at `/` and an API at `/api/*`,
backed by a Durable Object.

## When to call me
- _(concrete example prompts + who uses the UI)_

## Non-goals
- _(what this agent should NOT do)_

## Tools
- _(list tools, keep under 10)_

## UI
- Static assets live in `public/`. Replace `index.html` with your real UI.
- Vite + React works well — build to `public/` in your `build` script.

## Shared packages
- `@agentbuilder/core`
- `@agentbuilder/llm`

## Behavioral guidelines
- Prepend `CORE_BEHAVIORAL_PREAMBLE` from `@agentbuilder/llm` to this agent's system prompt.
- Full guidelines: see `/CLAUDE.md` at the repo root.
