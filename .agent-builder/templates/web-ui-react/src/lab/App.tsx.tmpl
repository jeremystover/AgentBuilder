// Replace this with your agent's UI. The single panel here is just a
// placeholder so the build succeeds.

export function App() {
  return (
    <div className="h-screen flex items-center justify-center bg-bg-primary text-text-primary">
      <div className="text-center space-y-3">
        <h1 className="text-3xl font-semibold">{{AGENT_NAME}}</h1>
        <p className="text-sm text-text-muted">
          Replace this placeholder with your SPA. See README.md for the
          shared conventions.
        </p>
        <a href="/{{SURFACE}}/logout" className="text-xs text-text-muted underline">
          Sign out
        </a>
      </div>
    </div>
  );
}
