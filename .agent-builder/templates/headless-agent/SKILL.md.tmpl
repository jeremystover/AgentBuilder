# __AGENT_NAME__

**Purpose.** __AGENT_PURPOSE__

## When to call me
- _(fill in concrete example prompts this agent handles)_

## Non-goals
- _(things this agent should NOT do — these drive routing and prevent overlap)_

## Tools
- _(list tools by name; keep under 10)_

## Shared packages
- `@agentbuilder/core`
- `@agentbuilder/llm`

## Notes
- Model tier: `default` (Sonnet). Drop to `fast` (Haiku) for classification-only paths.
- Prompt caching: on by default via `@agentbuilder/llm`.
